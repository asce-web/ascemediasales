<div{{ attributes }}>
  <div class="section-inner">
  <div{{ attributes.removeClass('inv-section').removeClass('inv-sticky').removeClass('unsticky-mobile').addClass(container_class)|without('id')|without('style') }}>
    <div class="row">{{ content }}</div>
  </div>
  </div>
</div>
